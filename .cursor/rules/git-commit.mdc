---
description: 작업 완료 시 항상 커밋 메시지(+ PR 초안) 제안. 필수 동작.
alwaysApply: true
---

# Git & Commit Rules

## ⚠️ 필수: 작업 끝나면 항상 커밋 메시지 추천

- **사용자 요청 작업을 마쳤을 때는 반드시(항상) 응답 맨 끝에 커밋 메시지를 제안한다.** 건너뛰지 않는다.
- 작업이 “완료”된 것으로 보이면(요청한 수정·추가가 반영되었으면) **무조건** 제안한다. 테스트/린트를 실행하지 않았어도, 실행했는데 실패해도 **일단 제안은 항상 한다.**
- 제안할 때: 실제로 변경한 파일·내용을 기준으로 Conventional Commits + Gitmoji 형식으로 작성한다.
- Base everything on **real changes** (diff/edited files), not assumptions.
- Use **Gitmoji 기반 Commit Type** for commit/PR title. Choose the type that best matches the change:

  | 이모지 | type       | 용도                                  |
  | ------ | ---------- | ------------------------------------- |
  | ✨     | `feat`     | 새로운 기능 추가                      |
  | 🐛     | `fix`      | 버그 수정                             |
  | 💡     | `chore`    | 자잘한 코드 수정 (주석, 포맷 등)      |
  | 📝     | `docs`     | 문서 수정 (README 등)                 |
  | 🚚     | `build`    | 빌드 시스템, 패키지 관련 수정         |
  | ✅     | `test`     | 테스트 코드 추가/수정                 |
  | ♻️     | `refactor` | 코드 리팩터링 (기능 변화 없음)        |
  | 🚑     | `hotfix`   | 긴급 수정 (배포 중 발생한 문제 등)    |
  | ⚙️     | `ci`       | CI/CD 관련 변경 (GitHub Actions 등)   |
  | 🔧     | `config`   | 설정 파일 수정 (env, settings 등)     |
  | 🗑️     | `remove`   | 불필요 파일/코드 삭제                 |
  | 🔒     | `security` | 보안 관련 수정                        |
  | 🚀     | `deploy`   | 배포 관련 커밋                        |
  | 🧩     | `style`    | 코드 스타일 변경 (공백, 들여쓰기 등)  |
  | 🎨     | `ui`       | UI, 스타일 관련 변경 (템플릿, CSS 등) |
  | 🔄     | `sync`     | 코드/데이터 동기화                    |
  | 🔥     | `clean`    | 코드/로그 정리                        |

- Commit 메시지에는 **반드시 이모지 + type** 조합 사용 (예: `✨ feat(scope): 요약`). **추천 시 이모지를 꼭 넣어 제안한다.**
- Include **affected scope** when possible (e.g. `provider-app`, `demand-app`, `api_server`, `ai_server`).
- Keep commit subject and PR title **under 72 characters**.
- **Do NOT run git commands automatically.** Only suggest; wait for user confirmation.

## 완료 기준 및 참고 사항

- **제안은 항상 한다.** 아래 조건과 무관하게, 작업을 마쳤다면 커밋 메시지(및 PR 초안)를 제안한다.
- 다만 다음에 해당하면 제안 **바로 아래**에 한 줄 안내를 넣는다:
  - 빌드/컴파일 실패, 문법·타입 에러가 있음 → "빌드/타입 에러 해결 후 커밋 권장"
  - 린트 에러가 남아 있음 → "린트 수정 후 커밋 권장"
  - 테스트를 아직 안 돌렸거나 실패함 → "테스트 통과 후 커밋 권장"
- 즉, **추천 자체는 누락하지 않고**, 필요 시 "이것만 확인하고 커밋하세요"만 덧붙인다.

## Language

- If the user prefers Korean, write the **summary** (after the type/scope) in Korean; keep type and scope in English (e.g. `🐛 fix(provider-app): 견적 페이지 import 오류 수정`). PR body can be in Korean.

---

## Commit Message Output Format

```
✨ feat(scope): short summary

- key change 1
- key change 2
```

(이모지는 **필수**: 해당 type에 맞게 선택하여 항상 넣어 제안. ✨ feat, 🐛 fix, 📝 docs, ♻️ refactor, 🔧 config 등.)

## PR Title + Body Output Format

After offering the commit message, also offer a **PR title** and **PR body** in this format:

```
## PR Title
✨ feat(scope): short summary
(commit과 동일한 한 줄, 이모지+type 유지)

## PR Body

### 변경 사항 (What changed)
- key change 1
- key change 2

### 영향 범위 (Scope)
- provider-app / api_server / ...

### 확인 방법 (How to test) — 선택
- 로컬에서 [어떤 경로/동작] 확인
```

- PR title: use the same line as the commit subject (or a slightly more descriptive one if needed).
- PR body: list concrete changes, affected scope, and optional “how to test” so the reviewer can verify.

---

**작업을 마쳤을 때:**

1. 작업 완료를 한 문장으로 말하고,
2. **반드시** 그 다음에 **제안 커밋 메시지**를 위 형식대로 출력하고,
3. 가능하면 **PR 제목 + 본문 초안**도 같은 블록 안에 출력한다.  
   사용자가 복사해 `git add` / `git commit` 하고 PR을 열 수 있도록 한다. 이 단계를 생략하지 않는다.
